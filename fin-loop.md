# 一、业务背景：为什么要做这个

**一句话：财经微服务的隐藏问题，靠人工巡检永远覆盖不过来，而且这类问题一旦爆发就是对不上账。**

## 1. 财经域的底线

财经业务的铁律是**资金核算必须准确**——每一笔往来、每一张发票、每一次清账，金额都不能错。这决定了系统对代码质量的要求远高于普通业务：一个隐藏缺陷可能不报错、不影响功能，却在默默影响核算准确性。

## 2. 维护困境（面试开场可以直接报数字）

- 多个财经微服务，**独立仓库、独立数据库，共用一个 ELK 索引**；
- 7 天积累 **19 万条 ERROR**，散落在各应用，值班只看得到热门几条；
- 一个对账流程跨 4-5 个应用，排查要切多个仓；
- 经验在对话里，关掉就丢——A 应用踩的坑 B 应用还会再踩。

## 3. 最致命的是"沉默的问题"（三个真实例子）

| 症状 | 为什么人发现不了 |
|---|---|
| 226 条 ERROR，message 只剩一个公司编码（slf4j 参数错位，错误信息全丢） | 不报错、不影响功能，谁会去逐行审 log.error 参数顺序？ |
| 一条 COUNT 慢 SQL 在生产跑了 42 秒 | 不报错只是慢，没人主动查慢 SQL |
| null 兜底代码看着对，实际位置错了，异常没被兜住 | 代码 review 看不出"意图对、实现错" |

人工排查一条 ERROR 的完整流程（复制日志→喂 AI→对代码→写修复→跑测试→提审查）要 **2-3 天**，只能覆盖冰山一角。

## 4. 还有一条隐线：月结

月结是财经的高压窗口期。两类问题在月结期间集中爆发：线上报错、业务用户的月结问题问询（"这笔往来为什么清不掉"）。传统靠值班人工响应，fin-loop 把这条也自动化了（IM @我 消息自动触发诊断）。

---

# 二、方案定位：一句话 + 全局链路

**一句话**：fin-loop 是财经微服务的"循环引擎"——让「发现 → 交付 → 验证 → 持久化 → 调度」这个循环自己转起来：每天自动体检，发现人发现不了的问题，定位根因、自动修复到提 MR、知识沉淀复利。**自动化止步于提 MR，预发及以后全人工门禁。**

一条 ERROR 的完整生命周期：

```
launchd 9:30 触发 → 脚本扫 ELK（聚类/抽样/关键词匹配，零 token）
→ 产巡检报告 + push → 后台触发 LLM 诊断
→ 命中 fix-kb 已知模式？命中=套模板走快路；未命中=深诊断（读代码定位 file:line + 根因分类）
→ 产诊断报告 → Phase 7：git blame 找修改人 → 生成工单 → IM 通知
→ trigger_autofix：按 app 分组并行自动修复（起 fix 分支 → 改代码 → 编译 → 资金审计）
→ 验证闸口全通过 → 提 MR + 通知 Owner → 归档时沉淀新模式回 fix-kb
→ 反哺下次诊断（闭环）
```

---

# 三、技术架构

## 1. 三层分离（先讲这个，体现架构观）

| 层 | 内容 | 为什么这么分 |
|---|---|---|
| **fin-loop 仓**（核心共享层） | 巡检脚本、Skills、fix-kb、reports、tickets | 信息归集点，版本控制团队共享，软链到 `~/.OpenCode` 全局生效 |
| **各应用仓**（应用层） | 各自代码 + openspec/changes | 代码归属独立，改动走各自 openspec + MR，不跨应用合代码 |
| **共享基建** | ELK / IM / OpenCode | 多应用共用 |

明确不用 git submodule 聚合多仓（运维负担高、混淆只读/可写边界），跨应用读代码用绝对路径只读访问。

## 2. 六组件分层

| 层 | 组件 | 职责 |
|---|---|---|
| 调度层 Automations | launchd 工作日 9:30 + 每 10 分钟告警轮询 | 让发现不依赖人 |
| 感知层 Connectors | elk-query、git-context、IM-cli、**sql-query（数据查询网关）** | 让 Agent 看见线上世界 |
| SOP 层 Skills | bug-diagnose（7 阶段）、bug-autofix（6 步）、ticket-fix、perf-diagnose/autofix | 把经验固化成 SOP |
| 验证层 Sub Agents | 独立 Agent 代码审查 + 资金影响审计 + 规则库校验 | **修复者不能给自己打分** |
| 持久化层 State | fix-kb + learnings + reports + tickets + archive | 经验落到对话外 |
| 隔离层 Worktrees | 每个 bug 独立 fix 分支（从 origin/master 起） | 多 bug 并行不冲突 |

## 3. 四条闭环（触发方式）

1. **巡检闭环**：launchd 定时，无人值守，全链路到 MR；
2. **告警闭环**：IM_alert_watch 每 10 分钟轮询 IM 告警群，解析 TraceId/堆栈 → 查 ELK 对比 → 可修提 MR、不可修给结论；
3. **聊天监听闭环**：IM cli 监听 @我，线上问题/月结问题问询自动触发——业务用户群里提问，Agent 自动诊断回复；
4. **手动闭环**：贴工单消息触发 ticket-fix。

## 4. 模型栈（底座，讲清楚"用了什么"而不是"造了什么"）

Claude Code CLI（agent 骨架：skill 机制/工具调用/会话管理/`--bg` 非交互）→ OpenCode Hub（协议适配/路由/会话录制）→ GLM-5.2（实际推理）。

**为什么是这个组合（面试必问）**：
- **数据合规**：财经日志全是金额/税号/银行账号，不能出境，GLM 走国内通道是前提；
- **骨架与推理解耦**：要的是 Claude Code 的工程能力，推理换成合规可达的国产模型；
- **成本命门**：实测 prompt caching 透代理层照样命中，单请求 37 万+ token 的重复前缀（巡检报告、SKILL 定义）走缓存不吃全价。

---

# 四、技术要点（面试的主战场，逐个能展开）

## 要点 1：脚本 vs Agent 分工——确定的活不给模型

判断标准一句话：**这一步的输出是确定的吗？** 确定的（聚类、计数、关键词匹配、git blame、分支检测）给零 token 常驻脚本；需要"理解"的（读代码定位、判断业务意图、生成补丁）才给模型。

收益：19 万条 ERROR 的扫描聚类零模型调用；巡检可 launchd 常驻不需要会话；token 只花在刀刃上。

## 要点 2：上下文工程——把窗口当稀缺资源

- **结构化诊断上下文**：脚本在调用模型前组装好——日志聚类标签、抽样堆栈、TraceId 与请求体、应用/owner（git blame）、file:line、历史同类请求对比、fix-kb 命中先验；
- **裁剪**：非交互诊断模式下，巡检报告里已标注"命中已知模式"的 logger，模型直接跳过连代码都不读；只有新模式/突增才走深度诊断。一次巡检聚出几十个 logger，真正要深读的往往只有几个。

## 要点 3：提示词是数据契约，不是聊天

自动闭环里模型输出是给**下游脚本 parse** 的，所以：
- **收紧**：根因分类在提示词里锁死 6 个枚举值（数据Bug/代码质量/外部系统异常/数据权限异常/基础设施抖动/预期行为误报），甚至写明"不要写成 XX"；
- **容错**：即便如此模型仍会漂移（加粗、带前缀后缀），下游正则写得非常宽容；
- **双保险铁律**：只信提示词约束，迟早被一次漂移打断整条链路。

## 要点 4：不信任 Agent 自觉——编排脚本兜底

实测后台 agent 会"自信地跳步"：跑完分析就结束，漏写结果 json、漏发通知。所以设计是：**agent 只负责改代码 + push，结果落盘和通知由编排脚本用 git 检测分支状态兜底**（有远程分支=提了 MR；有本地提交无 push=push 失败；无提交=转人工）。

## 要点 5：数据查询合规层（sql-query，你建设的工具）

Agent 排查时日志看不到数据面（金额状态、单据状态），需要查库——但**绝不能让 LLM 直连生产库**。方案：把数据库查询封装为业务接口形态的查询服务，四层防护：

1. **DB 侧**：只读 replica 不碰主库，动态脱敏（金额末位扰动、税号/银行账号/身份证掩码），专用账号列级权限；
2. **网关**：SQL AST 校验仅放行单条 SELECT、禁 DDL/DML、强制 LIMIT、禁 SELECT *、列白名单，敏感列命中即掩码表达式替换；
3. **返回**：结果集兜底二次扫描；聚合优先于明细、EXPLAIN 优先于查数据、采样优于全量；
4. **流程**：每条 SQL/命中表列/耗时/traceId 全留痕可审计；低敏自动放行，高敏（明细/跨表 join/金额账户表）走人工查询工单。

一句话：**能聚合就不查明细，能脱敏就不碰明文，模型只看结构、量级、分布，不看原始敏感值。**

## 要点 6：验证闸口——模型会自信犯错，验证器分层否决

这是财经场景最要命的一条，用慢 SQL case 讲（见案例 1）。五个闸口：

1. 资金影响审计：扫 diff，触及金额/税率/结算/入账/改返回值语义 → 作废转人工；
2. openspec 强制流程：spec→plan→review→apply，人工 review 硬门禁；
3. 单测 + 编译：最多 3 轮，失败降级人工；
4. SQL 变更审计：AST 比对不改语义，DDL 不自动跑走 DBA 工单；
5. 风控降级：3 轮失败/资金路径 → 停止，建工单推 Owner，不在错误方向空转。

**边界判定金句（背下来）**：如果放宽校验会让本该失败的情况静默通过，那就是业务校验，绝不改。

## 要点 7：双层知识体系——故障模式库 + 领域本体规则库

**① fix-kb 故障模式库（"怎么修"）**：YAML 模式卡（症状关键词 → 修复模板，含 exclude_keywords 防误标），修复归档满足严格门槛（bug-fix + 修复成功 + 可泛化）自动沉淀；诊断先查库，命中走快路跳过深度分析。这不只是检索增强——它**改变模型下一次的决策路径**：有先验走快路，没先验走慢路。实测跨应用命中：account-api 命中 nyq-api 沉淀的模式。

**② 领域本体规则库（"什么不能动"）**：以往来模块为例，建模「挂账→ 开票→ 清账→ 抵销的实体关系与硬约束，结构化为规则卡，每条含 **intent（为什么）/ scope（边界）/ counter-intuition（反直觉点）/ antiPatterns / codeAnchor（方法+签名锚点）**，由 rule-reviewer 在评审 diff 时按域加载校验。例如：

- **金额守恒恒等式**：含税总额 ≡ 已清账 + 清账余额；可清账守恒——这不是冗余校验，是给审计证明资金守恒的公式，破坏了是最高级事故；
- **开票-清账依赖**：需开票的往来入池时可清账额度必须为 0，等开票回调解锁——清账凭发票核销单，没发票就清账是跳过税务依据凭空打钱；
- **状态机**：终态（PAYED/REJECTED/REVOKED）不可回退——允许回退 = 允许重复打款。

这层解决了 Agent 乱改业务代码的根本问题：诊断和评审时能区分"代码缺陷"与"业务校验"。

## 要点 8：财务红线（体现领域敬畏）

自动化止步于提 MR，预发及以后全人工；金额/税率/结算/入账路径一律降级人工；巡检脚本只读不写；DDL 走 DBA 工单（pt-osc），650 万行表不能让 Agent 直接 ALTER；**承认半自动**——人卡在关键决策点，不是卡在重复劳动点。

---

# 五、实战案例（STAR 故事，面试最加分的部分）

## 案例 1：42 秒慢 SQL——验证器否掉生成器的假设（必讲）

巡检发现 xxx 表 COUNT 查询跑了 42 秒（一直都在，没人查过）。三层纠偏：

1. Agent 静态分析一口咬定是 WHERE 里 `OR (id IS NULL)` 破坏索引——推理链通顺，**但假设不是证据**；
2. dev 库 EXPLAIN `rows=1`（数据量小复现不了），差点又被误导；
3. 生产库 EXPLAIN：`type=ALL, key=null, rows=6,500,510, filtered=0.19%`——全表扫 650 万行，真正根因是**缺联合索引**，跟 `id IS NULL` 毫无关系。

加索引的 DDL 走 DBA 工单 pt-osc，不自动跑。**金句：Loop 的可信度不来自模型多聪明，来自验证器分层否决。**

## 案例 2：226 条 ERROR 信息全丢

message 只剩公司编码 "xxx"。Agent 读代码发现 `log.error(req.getCodeCompany(), "{}步骤执行失败", ...)`——SLF4J 把第一个参数当模板，真实错误信息全部丢弃。**不报错不影响功能，只是排查时抓瞎**——这种沉默 bug 只有每天自动巡检 + 读代码才能发现。3 处同 pattern 一次修，沉淀 `slf4j-arg-misuse`。

## 案例 3：被拦下的误判（比成功修复更有价值）

Agent 把 `checkArgument(map.size() == 1)` 判成"断言过严"，生成了放宽校验的补丁。人工 review 发现这是**业务校验**——公司唯一性保护，放宽会让数据风险静默通过。修复被拦下。教训固化成判定标准和审计 checklist。

## 案例 4：治标不治本的教训

xxx NPE，Agent 只读堆栈+代码就判"加 null 校验"提了 MR。后端反馈：正常情况不该是 null。深查 ELK 历史请求对比发现真正根因是**前端偶发漏传 params 字段**——后端加校验会掩盖前端 bug，MR 关闭。固化成诊断铁律：**必须查 ELK 历史同类请求对比，区分代码缺陷 vs 调用方传参；根因在调用方/外部/拿不准 → 不提 MR 转人工，宁可少修不可错修。**

## 案例 5：工程踩坑——分支基线污染

某次修复在 test 分支上 `checkout -b`，fix 分支带进 **514 个无关提交**，MR diff 全污染。固化成红线：fix 分支一律从 `origin/master` 起，push 前自检 `merge-base --is-ancestor`。体现"教训 → 机制"的工程文化。

---

# 六、高频面试问题与建议回答

**Q1：为什么不让 Agent 全自动到底（自动部署）？**
财经资金不可错。我们的取舍是自动化止步于 MR：Agent 做发现和搬砖，人做判断。可信度靠验证器分层否决，不是靠模型聪明。全自动不是目标，**可控的自动化**才是。

**Q2：LLM 输出不稳定，你怎么保证链路可靠？**
三层：① 提示词锁死输出契约（枚举值）；② 下游宽容解析兜漂移；③ 编排不信任 Agent 自觉，用 git 状态检测兜底结果。契约收紧 + 执行兜底，缺一不可。

**Q3：成本怎么控制？**
确定的活给脚本（零 token）；已知模式走 fix-kb 快路跳过深诊断；prompt caching 透代理命中（37 万 token/单请求的重复前缀走缓存）。原则：**模型贵在推理不在读字，喂它该理解的，别喂它能计算的。**

**Q4：怎么防止 Agent 改坏业务代码？**
四道防线：资金 diff 审计（触及金额/入账作废）；领域本体规则库（区分缺陷与业务校验，codeAnchor 锚到方法级）；SQL AST 语义比对；人工 review 硬门禁。外加判级：能修的才修，拿不准的转人工。

**Q5：这套东西的效果怎么衡量？**
ERROR 趋势持续下降、慢 SQL 逐步消除；人发现不了的沉默 bug 被挖出（226 条信息丢失、42s 慢 SQL）；一个应用修的 bug 跨应用复用（fix-kb 命中）；值班从"被动救火"变成"关键闸口做判断"。

**Q6：和 RAG / 传统告警平台的区别？**
传统告警只到"通知人"；我们是"发现→定位→修复→沉淀"闭环。fix-kb 不是检索增强，是改变模型决策路径的先验——命中走快路。沉淀有严格门槛（可泛化），不是把对话存下来就完事。

**Q7：局限性/下一步？**
跨应用调用链诊断（一个对账流程跨 4-5 个应用，考虑只读 worktree 让 Agent 同时看多仓）；fix-kb 跨团队复用；资金路径从"全人工"探索"Agent 出方案 + 人决策"的半自动；告警聚合降噪。

**Q8：为什么不直接用现成的 AIOps 平台？**
财经领域的判定规则（业务校验 vs 缺陷、资金红线、月结判级）是领域知识，现成平台给不了；我们的方案把领域知识固化成 Skills + 规则库 + fix-kb 三类资产，团队可维护可复利，且模型骨架与推理可替换（合规约束下换模型不换资产）。

---

# 七、数字记忆清单（被追问时脱口而出）

| 数字 | 含义 |
|---|---|
| 9 个应用 | 财经微服务覆盖范围 |
| 19 万条 / 7 天 | ELK ERROR 总量，共用 1 个索引 |
| 226 条 | 信息全丢的沉默 ERROR |
| 42 秒 → 650 万行 | 慢 SQL → type=ALL 全表扫 |
| 6 类 | 根因分类枚举 |
| 5 道闸口 | 验证分层 |
| 最多 3 轮 | 单测重试上限 |
| 4 个模式 | fix-kb 沉淀（3 active + 1 draft），9 万/天噪音降级 |
| 37 万+ token | 单请求 prompt cache 命中 |
| 514 提交 | 分支污染教训 |
| 10 条 critical | 往来清账域本体规则（恒等式/状态机/事务原子性） |

---

**面试叙事主线（30 秒版）**：财经微服务有大量人工发现不了的隐藏问题，我建了 fin-loop——一个多源触发、脚本与 Agent 分工的诊断-修复自动化闭环。核心不是让 AI 多聪明，而是三件事：确定性的活零 token 交给脚本、模型输出当契约不当结论用、资金路径用分层验证器否决。自动化止步于 MR，知识沉淀成可复用资产，一个应用踩的坑所有应用避雷。

fin-loop 的 Bug 自动修复靠什么做到可靠
财经场景的底线是"资金不可错"，所以 fin-loop 的可靠性不来自"模型多聪明"，而来自一套刻意保守的工程约束。可以归纳为六道防线：
一、根本设计原则：*"生成器不能给自己打分，必须有验证器分层否决"*
设计文档心得五定死了一条铁律：
绝不让模型的单点判断直接落地。 模型是"生成器"，它旁边必须站着一排"验证器"：静态分析、dev EXPLAIN、生产 EXPLAIN、单测、人工 review——层层否决。Loop 的可信度不来自"模型多聪明"，来自"验证器分层否决"。
这是整个可靠性的哲学基础。下面五道防线都是它的具体实现。
二、防线1：边界收得极窄——只允许"防御性改动"
bug-autofix / ticket-fix 的红线（硬约束，命中即终止）：
- ❌ 不改金额/税率/结算/入账主链路
- ❌ 不改 pom.xml 依赖、*-dal 的 SQL/Mapper、logback.xml
- ✅ 只允许：日志级别、try-except、参数校验、SpEL 模板、log 参数顺序
自动修复的目标不是"什么都能修"，而是只碰绝对安全的防御性改动，把风险面从源头压到最小。真正的资金逻辑一律不自动碰。
三、防线2：资金影响审计 checklist（提交前必做）
每条补丁 push 前独立扫 diff，任一命中即作废转人工：
- 触及金额/税率/结算/入账路径？→ 放弃
- 改变了方法返回值的业务语义？→ 放弃
- 只在日志/异常/参数校验层？→ 通过
配套一条关键判据（来自 005 真实误判）：
如果放宽校验会让本该失败的情况静默通过，就是业务校验，绝不改。
实测 T37（僵死清账单）、T42（预收余额缺失）等工单，agent 都精准判定触及资金红线，零改动零 push，转人工。
四、防线3：*"找真正根因再动工"，禁止治标方案*
来自 account-api GL 凭证 NPE 的真实教训：agent 只读堆栈就判"加 null 校验"提了 MR，后端反馈"正常 query 不该是 null"，深查 ELK 才发现真根因是前端偶发漏传 params——加 null 校验会掩盖前端 bug。教训固化进诊断 prompt：
必须查 ELK 历史同类请求对比（正常 vs 异常请求体），区分"代码缺陷"和"调用方传参问题"；根因在调用方/拿不准 → 不提 MR，宁可少修不可错修。
五、防线4：fix 分支基线红线 + push 前自检（防 MR 污染）
来自 T4 真实踩坑（从 test 分支起 fix，污染 514 个提交）。固化为：
- 脚本硬编码 git checkout origin/master -b fix/...，无论当前在哪个分支
- push 前自检：git merge-base --is-ancestor origin/master HEAD，git log origin/master..HEAD 应只含本次 1 个提交
保证 reviewer 看到的 diff 干净、只含本次修复。
六、防线5：结果判定不信任 agent 自报，用 git 事实兜底（最硬核的一条）
这是 autofix_tickets.py 的核心可靠性设计。后台 agent 会"自信地跳步"——跑完分析就结束，漏写结果、漏发通知。所以编排脚本不信 agent 说了什么，只信 git 分支的客观状态（detect_fix_result）：
git 检测到的事实	判定 status
远程有 fix 分支 = 已 push	ok（提了 MR）
有本地提交但没 push	local（push 失败，需查）
无提交 / 无分支	fail（红线/编译失败/转人工）
而且当 agent 自报状态与 git 冲突时，以 git 为准（agent_r.get("reason") 只采用它的文字原因，状态判定始终用 git）。甚至还能区分"agent 根本没跑起来"（无日志）vs"agent 跑过判定红线"（有日志）——来自 T46 实测（后台环境启动失败却套用红线文案误导人）。
通知也由编排脚本发，不靠 agent：popo_cli_send 把 MR 链接（成功）或转人工原因（失败）直接推给工单负责人。
七、防线6：3 轮失败即停 + 全流程走 mixspec 规范
- 重试上限：编译/单测失败最多 3 轮，仍失败 → 停止、保留本地改动、转人工，不在错误方向空转
- mixspec 强制流程：所有代码改动走 spec → plan → review-code → apply，人工 review 是硬门禁，可审计
- 自动化边界：自动只到 MR，预发及以后全人工门禁（不像阿里那套自动到预发）——资金不可错，人卡在关键决策点
八、失败降级路径全覆盖（不留静默失败）
ticket-fix 对每种异常都有明确降级：clone 失败→报错、fix-kb 未命中→走通用判断、编译失败(类型改动)3轮→转人工、push 失败→保留 commit 提示、拿不准→转人工。任何一步失败都有落点，不会静默烂尾。
一句话总结
fin-loop 的自动修复可靠，是因为它把"信任"从模型移到了工程约束上：
1. 窄边界——只改防御性代码，资金逻辑一律不碰
2. 多验证器分层否决——资金审计 → mixspec review → 编译/单测 → 人工，每层都能推翻上一层
3. git 事实兜底——结果判定不信 agent 自报，只信分支客观状态
4. 保守边界——自动只到 MR，预发后全人工；3 轮失败即停、宁可少修不可错修
5. 教训固化——每个真实误判（005 业务校验误判、T4 分支污染、GL NPE 治标、T46 假红线）都被写死成脚本硬编码或 checklist
核心信条就是那句话：用模型的推理，别信模型的结论；每一个 fix 落地前，都要经过一排推不翻它才算数的验证器。
